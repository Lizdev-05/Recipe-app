 <div class="flex flex-col m-10 gap-4 items-center justify-center">
 <h1 class="text-xl font-bold text-center text-gray-700">Recipes</h1>
    <div class="bg-gray-900 hover:bg-gray-800 rounded-lg py-2 text-center cursor-pointer mb-4 font-bold text-gray-100 p-5" >
      <%= link_to "New recipe", new_recipe_path, class: "btn btn-outline-success btn-sm" %>
    </div>
 <% @recipes.each do |recipe| %>
            <%= render recipe %>
        <% end %>
  </div>